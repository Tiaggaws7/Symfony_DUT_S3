{% extends 'lesListesDeStages.html.twig' %}

{% block title %}stages de l'entreprise numéro {{entreprise.id}}{% endblock %}

{% block table %}

    {% for stage in stages %}

        {% if stage.EntreprisesStages.id == entreprise.id %}
            <tr>
            <th scope="row"> {{ stage.id }} </th>
            <td> <a href="{{ path('detailStage', {id:stage.id}) }}"> {{ stage.titre }} </a></td>
            <td>{{ stage.mission }}</td>
            <td>{{ stage.email }}</td> 
        </tr>
        {% endif %}
    {% endfor %}

{% endblock %}